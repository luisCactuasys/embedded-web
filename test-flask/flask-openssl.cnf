[req]
distinguished_name = req_distinguished_name
x509_extensions = v3_req
prompt = no

[req_distinguished_name]
C = US
ST = California
L = San Francisco
O = My Company
OU = My Department
CN = my-embedded-board.local
emailAddress = admin@mycompany.com

[v3_req]
subjectAltName = @alt_names

[alt_names]
DNS.1 = my-embedded-board.local
IP.1 = 192.168.1.104
